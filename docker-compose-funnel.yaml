services:
  funnel:
    image: quay.io/ohsu-comp-bio/funnel:latest
    ports:
      - "8000:8000"
    command: server run --AmazonS3.Disabled=false
    environment:
      # read from .env file
      - AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID}
      - AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY}